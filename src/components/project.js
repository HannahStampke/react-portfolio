import React, {useState} from "react";

import {
    /* TODO: ADD Styles from the style file  */
} from ".../styles/projectStyle";

export default function Project({project}) {

    return (
        /* TODO: Add all the project content here once */
    );
}